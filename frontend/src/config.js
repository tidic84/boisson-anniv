// Configuration de l'application
const config = {
  // Base path pour les routes React Router
  basePath: '/anniv-emma',

  // Base URL pour les appels API
  apiBaseUrl: '/anniv-emma/api'
};

export default config;
